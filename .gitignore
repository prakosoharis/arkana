# Dependencies and build output
node_modules/
.next/
coverage/
dist/
__pycache__/
.pytest_cache/
.venv/
*.py[cod]

# Environment and local database state
.env
.env.local
postgres-data/

# Market data: only small fixtures are committed
data/raw/*
!data/raw/.gitkeep
data/processed/*
!data/processed/.gitkeep

backups/

# ARK-S24-06: the test suite database. It mutated on every run, which is
# why it kept appearing in git status; the suite now builds its own schema in
# /tmp and no longer depends on this file.
services/research/arkana_metadata.db
